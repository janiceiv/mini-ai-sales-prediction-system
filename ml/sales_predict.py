# -*- coding: utf-8 -*-
"""sales_predict.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1DnSupYHED_B25nfq9KiJIkNCq08dPmfb
"""

from google.colab import files
uploaded = files.upload()

import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LogisticRegression
import joblib

#load dataset
df = pd.read_csv("sales_data.csv")
print(df)

#ekstrak label
df['status'] = df['status'].map({'Tidak': 0, 'Laris': 1})


X = df[['jumlah_penjualan', 'harga', 'diskon']]
y = df['status']

#split jadi data train dan test
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

#train model
model = LogisticRegression(max_iter=1000)
model.fit(X_train, y_train)

#evaluasi model dengan metrik akurasi
print("Accuracy:", model.score(X_test, y_test))

#simpan model
joblib.dump(model, "model.pkl")
files.download("model.pkl")

